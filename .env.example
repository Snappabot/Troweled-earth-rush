# Supabase backend for CONTRACT WARS leaderboard
# Create a free project at https://supabase.com
# Run the SQL below in the Supabase SQL editor, then paste your keys here

VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY

# SQL to create the scores table:
# CREATE TABLE tem_scores (
#   id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
#   player_name text NOT NULL,
#   job_title text NOT NULL,
#   crew_ids text[] NOT NULL,
#   completion_time_s integer NOT NULL,
#   payout integer NOT NULL,
#   created_at timestamptz DEFAULT now()
# );
# ALTER TABLE tem_scores ENABLE ROW LEVEL SECURITY;
# CREATE POLICY "Anyone can insert" ON tem_scores FOR INSERT WITH CHECK (true);
# CREATE POLICY "Anyone can read"  ON tem_scores FOR SELECT USING (true);
